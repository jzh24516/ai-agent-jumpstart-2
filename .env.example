# Do not store the raw password here. Generate a salt:hash value with Node's scrypt API.
WORKSHOP_MAKER_PASSWORD_HASH=your-salt:your-scrypt-hash
# Use a random 32-byte value to sign the short-lived local maker session.
WORKSHOP_MAKER_SESSION_SECRET=replace-with-a-random-secret